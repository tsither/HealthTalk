def main():
    print("Test")

    # 1. Takes csv (filename / unit / info / info) and saves as dictionary
    # 2. Goes to folder with jsons and saves the info from the filename (filename / config)
    # 3. Takes json and creates a dictionary based on results
    # 3. Tests accuracy and f1score comparing 1 and 3, saves evaluation in dict from 2
    # 4. Continues until all jsons have been read
    # 5. Saves dictionary with file info and test results into a csv
    # 6. Based on results, we use averages to get the best configuration

if __name__ == "__main__":
    main()